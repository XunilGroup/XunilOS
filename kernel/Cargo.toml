[package]
name = "limine-rust-template"
version = "0.1.0"
edition = "2024"

[dependencies]
font8x8 = { version="0.3.1", default-features = false }
limine = "0.5"
micromath = "2.1.0"
spin = "0.10.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
